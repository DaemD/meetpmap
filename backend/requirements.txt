# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0  # Required by uvicorn[standard] for WebSocket support

# OpenAI API
openai==1.3.0
httpx<0.28,>=0.25.0
httpcore<1.0,>=0.18.0

# Configuration
python-dotenv==1.0.0

# Data validation
pydantic==2.5.0

# ML/AI dependencies
numpy>=1.24.3,<2.0.0
sentence-transformers>=2.2.2
scikit-learn>=1.3.2
# CPU-only PyTorch (smaller, Railway doesn't have GPUs anyway)
# Install with: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
transformers>=4.30.0

# Database
asyncpg==0.29.0  # PostgreSQL async driver

# Utilities
python-multipart==0.0.6
aiofiles==23.2.1  # For async file operations (future use)

# REMOVED (not used in main.py):
# rake-nltk==1.0.6          # Only in TalkTracesService (test file)
# keybert==0.8.1            # Only in TalkTracesService (test file)
# bertopic==0.15.0          # Not used anywhere
# neo4j==5.14.0             # Not used anywhere
# soundcard>=0.4.2          # Not used anywhere

