# Optimized requirements.txt for Railway deployment
# Reduces size from ~730 MB to ~450-500 MB

fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
openai==1.3.0
httpx<0.28,>=0.25.0
httpcore<1.0,>=0.18.0
python-dotenv==1.0.0
pydantic==2.5.0
numpy>=1.24.3,<2.0.0
sentence-transformers>=2.2.2
scikit-learn>=1.3.2
python-multipart==0.0.6
aiofiles==23.2.1
transformers>=4.30.0

# REMOVED (not used in main.py):
# rake-nltk==1.0.6          # Only in TalkTracesService (test file)
# keybert==0.8.1            # Only in TalkTracesService (test file)
# bertopic==0.15.0          # Not used anywhere
# neo4j==5.14.0             # Not used anywhere
# soundcard>=0.4.2          # Not used anywhere

# NOTE: For CPU-only PyTorch (saves ~200-300 MB), install separately:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# Or use the Railway build command:
# pip install torch --index-url https://download.pytorch.org/whl/cpu && pip install -r requirements.txt

